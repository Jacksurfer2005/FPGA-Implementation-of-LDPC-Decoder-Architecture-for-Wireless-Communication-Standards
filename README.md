### Sub-Block Breakdown:
1. **BRAM APP & CTV:** Divided into $K = N/z$ blocks for parallel access, where $N$ is codeword length and $z$ is circulant size. Dual-port block memories handle concurrent reads/writes.
2. **Operating Mode Selection:** Manages switching between "decoding" and "result output" modes, asserting the `READY` signal upon completion.
3. **Iterations Counter:** Monitors convergence equations ($Hx=0$) and iteration limits.
4. **VTC Calculation Block:** Computes variable-to-check messages by subtracting stored CTVs from current APPs ($VTC_n = APP_n - CTV_n$).
5. **Min/Submin Calculation Block:** Consists of:
   * **ABS Module:** Extracts signs and converts signed data to unsigned modulo values.
   * **Signs XORing Block:** Performs modulo-2 addition across sign bits.
   * **4-Stage Comparator Cascade:** Efficiently extracts minimum and subminimal values for row weights up to 7 (with zero/max-value padding for smaller weights).
   * **Sign Insertion Block:** Re-applies the computed global sign vector to the outputs.

---

## 📦 Optimized Parity-Check Matrix Storage Strategy

Because QC-LDPC base matrices ($H_b$) are generated by circular shifting of identity submatrices, storing full matrices is redundant. This architecture compresses $H_b$ into three vectors:

* `vals`: Stores all non-(-1) matrix elements (shift values).
* `pos`: Stores the matrix column index corresponding to each element in `vals`.
* `elementSize`: Stores the count of non-(-1) elements for each matrix row.

This approach significantly reduces distributed memory overhead while maintaining high-speed access during decoding cycles.

---

## 📊 FPGA Synthesis & Implementation Results

Implemented on the **Xilinx Zynq-7000 Mini-ITX Evaluation Board (XC7Z100-2FFG900)**. 

### Table I: Synthesis Results for IEEE 802.16e (WiMAX)
| Code Length ($N$) | Circulant ($z$) | Slice LUTs | Slice Registers | BRAM Blocks | Clock Frequency (MHz) | Throughput (Mbps - 1 Iteration) |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 576 | 24 | 80,260 | 37,113 | 216 | 240 | 483 |
| 960 | 40 | 108,761 | 51,964 | 248 | 214 | 703 |
| 1440 | 60 | 139,261 | 69,972 | 288 | 189 | 907 |
| 1920 | 80 | 159,265 | 88,463 | 328 | 195 | 1,219 |
| 2304 | 96 | 188,147 | 102,877 | 360 | 192 | 1,408 |

*Note: Peak decoder throughput reaches **1.2 Gbps to 1.408 Gbps** depending on configuration.*

---

## ⚖️ Performance Comparison with State-of-the-Art

| Architecture | Block Length ($N$) | FPGA Technology | Frequency (MHz) | Iterations | Throughput (Mbps) |
| :--- | :---: | :--- | :---: | :---: | :---: |
| Hailes et al. [8] | 576 – 2304 | Altera Stratix IV | 200 | 10 | 68.1 – 218.5 |
| Mhaske et al. [9] | 648 – 1944 | Xilinx Kintex 7 | 170 | — | 420 |
| Nimara et al. [10] | 1296 | Xilinx Virtex 7 | 288 | — | 102 |
| **This Work** | **576 – 2304** | **Xilinx Zynq-7000** | **192 – 288** | **10** | **48.3 – 140.8 (Multi-iter)** / **1,408 (Peak)** |

### Throughput Evaluation Formula:
$$T = \frac{F_{max} \cdot BL}{CPI \cdot NOI}$$
Where $F_{max}$ is maximum clock frequency, $BL$ is block length, $CPI$ is cycles per iteration, and $NOI$ is number of iterations.

---

## 📂 Repository Structure

```text
├── rtl/                          # Verilog/SystemVerilog Source Files
│   ├── top_decoder.v             # Top-level wrapper integrating all components
│   ├── vtc_calculation.v         # Variable-to-Check calculation module
│   ├── min_submin_calc.v         # 4-stage comparator & ABS logic
│   ├── ctv_app_calc.v            # Check-to-Variable and APP update unit
│   ├── mode_selector.v           # Operating mode & READY signal controller
│   └── iter_counter.v            # Iteration tracking & convergence monitor
├── memory/                       # Memory & Storage Modules
│   ├── bram_app.v                # Dual-port APP block RAM controller
│   └── matrix_rom.v              # Compressed matrix storage vectors (vals, pos, elementSize)
├── sim/                          # Testbenches & Simulation Scripts
│   └── tb_ldpc_decoder.v         # Comprehensive testbench for WiMAX/WiFi standards
└── constraints/                  # FPGA Pin & Timing Constraints (.xdc)